Docker MySQL image
	Server Name: mysqlserver
	password: sql_password23
	
SQL commands:

SHOW STATUS LIKE 'Uptime';
	This command displays the uptime of the MySQL server.
SELECT VERSION();
	This command displays the MySQL server version.
SHOW DATABASES;
	This command lists all the databases on the MySQL server.
CREATE DATABASE test_db;
	This command creates a new database named test_db.
USE test_db;
	This command selects the test_db database for further operations.
CREATE TABLE test_table (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(50));
	This command creates a new table named test_table with an id column and a name column.
INSERT INTO test_table (name) VALUES ('John'), ('Alice'), ('Bob');
	This command inserts some test data into the test_table table.
SELECT * FROM test_table;
	This command retrieves all the rows from the test_table table.
